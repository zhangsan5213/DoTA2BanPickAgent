# Python 环境使用规则

## 核心原则

**绝对禁止擅自操作**

## 环境使用规则

1. **禁止擅自操作**：
   - 永远不会擅自使用 pip、conda 等包管理工具
   - 禁止对 Python 环境进行任何未授权的修改

2. **默认执行环境**：
   - 所有代码执行默认使用：`E:\Anaconda\envs\genshin` 环境
   - 如需切换环境必须明确获得授权

3. **执行失败处理**：
   - 如果命令执行失败或无法执行，立即停止操作
   - 清晰地向用户报告问题，等待指示
   - 不进行任何尝试性的修复或替代方案

## 警告事项

- 任何违反上述规则的行为都将立即停止
- 所有操作都必须经过明确授权
- 保护用户环境的稳定性和安全性是首要任务
